import {NextRequest,NextResponse} from "next/server";import {supabaseAdmin} from "@/lib/supabaseAdmin";
export async function POST(req:NextRequest){const b=await req.json(),s=supabaseAdmin();const update:any={status:b.status,funded_amount:b.fundedAmount||0,updated_at:new Date().toISOString()}; if(b.status==="Funded") update.funded_at=new Date().toISOString(); const{error}=await s.from("leads").update(update).eq("id",b.leadId);if(error)return NextResponse.json({error:error.message},{status:500});if(b.note)await s.from("lead_notes").insert({lead_id:b.leadId,note:b.note});return NextResponse.json({ok:true})}
